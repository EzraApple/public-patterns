# Pipeline Worker

Future home of source adapters, scheduled ingestion, cheap detectors, and queue
consumers. It will own data arrival and candidate generation, not public request
handling or open-ended agent work.

This directory is intentionally documentation-only until the first source and
detector establish a real runtime contract.
