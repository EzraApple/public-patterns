# Investigation recovery and model comparison — September 9, 2026

## Production audit

The August 25–September 7 attempt ledger contained 18 distinct non-noise
candidates: eight were attempted and all failed; ten were never selected.
The latest daily summaries undercount those failures because later attempts
can replace a failed selection with another candidate or `not_ready`.
A lack of articles in this interval therefore does not demonstrate a lack of
worthwhile stories. One-candidate selection and readiness behavior remain
separate follow-up work.

After the DeepSeek balance was replenished, the August 26 Mission Bay traffic
stop and August 29 Chinatown private-graffiti cases were deliberately replayed
through the production Workflow. Both exhausted their retry with a terminal
OpenCode `Transport` failure near five minutes. No new article resulted.

The saved August 8 Bayview draft was briefly recovered, then removed after
full-text comparison showed that the existing August 17 story already covers
its two-intersection concentration. The private recovery is marked `discard`;
the original archive remains intact. Distinct dates and slugs are insufficient
proof of a new finding. The feed returned to four articles.

## Confirmed execution defects

- The pinned July OpenCode CLI let Bun's default five-minute HTTP deadline kill
  its long-running event stream. The [upstream August 19 fix](https://github.com/anomalyco/opencode/commit/d5bf8799c0e0706ec604324d9abef45cfae4dcd0)
  disables that deadline. The investigator now pins the npm `latest` release
  `@opencode-ai/cli@0.0.0-beta-17823`, which includes it. Live investigations
  continued beyond five minutes on that binary. The 12-minute sandbox and
  15-minute Workflow step limits remain intact.
- Exact DataSF queries returned 403 from `data.sfgov.org` and 200 from
  `data.sf.gov` on this date. The ingestion gateway and generated citations
  now use the working host. This is a dated observation, not a claim about an
  officially announced permanent redirect. Migration 0008 updates old-host links
  in current published article documents by adding a revision under the same
  slug and publication date; historical revisions remain intact. A local SQLite
  check verified body/source replacement, preservation, and repeat safety.
- The local paid-eval client also had an independent five-minute Undici
  response-header deadline despite a longer abort signal. Its long POST now
  uses Node's HTTP client with an explicit 16-minute abort.
- A utility under OpenCode's plugins directory was being loaded as a plugin
  without a default export. It now lives outside that auto-loaded directory.
- Failure classification now reads the terminal structured error before
  considering incidental tool output. Failed runs retain bounded, redacted
  unsubmitted drafts for diagnosis; these are never completed checkpoints.

## Duplicate controls

Publication uses an atomic D1 guard against a second slug for the same source,
day, kind, and area, including a replay with a different investigation ID.
Revisions under the existing slug remain possible. A real Worker/D1 integration
regression exercises this behavior.

New investigations receive up to 20 current articles, prioritizing the same
area, as full-text editorial context. The agent must discard an already-covered
finding unless a verified development materially changes it. The
`bayview-already-covered-2026-08-08` fixture tests cross-date overlap.
This is a bounded model check, not a semantic uniqueness guarantee; manual
backfills still require a final comparison with the current feed.

## Comparison design

All four published articles now have fixtures extracted from their original
successful R2 archives. This coverage was not automatic or complete before
this change. Expected outcomes, published slugs, and provenance labels stay
outside agent input. The original Haight traffic-stop negative control checks
that higher publication yield does not simply mean a looser story threshold.

Both models use maximum reasoning effort and the same 12-minute execution
budget. Inputs are frozen, but source queries are live; source drift and
editorial quality require human review in addition to sparse phrase checks.
Local evals do not publish. Infrastructure failures before model execution are
reported separately from model outcomes.

## Pricing and scope

On the [official pricing page](https://api-docs.deepseek.com/quick_start/pricing/),
V4 Flash off-peak prices per million tokens are $0.007 cached input, $0.22
uncached input, and $0.66 output; Pro is $0.022, $0.66, and $1.98 respectively.
Flash is about one-third of Pro's token price, not necessarily one-third of the
cost per completed article. The comparison runs and existing 22:30/23:30 UTC
cron schedule fall outside the weekday peak windows.

The [model updates](https://api-docs.deepseek.com/updates/) identify the current
aliases as V4-Flash-0731 and V4-Pro-0813. Flash-Vision-Exp adds experimental
vision without an advantage for the current text-only case input. The
[thinking-mode guidance](https://api-docs.deepseek.com/guides/thinking_mode/)
recommends high effort for everyday agents; this comparison holds effort at max
to isolate model choice. It does not establish the best effort setting.

## Observed results and model decision

Keep **V4 Pro / max** as the production default. Flash completed the same three
of four published-article cases, but the revised duplicate control returned a
correct discard on Pro while Flash timed out still drafting a second story.
This is enough to withhold an automatic-publisher switch, not proof that Pro
is globally better. Flash remains an explicit eval option at a lower token
price. Effort settings other than max were not tested.

Times below are archived sandbox execution durations, excluding local startup.
`investigate` means a submitted draft, not editorial approval. `deadline` means
the 12-minute Sandbox execution limit; it is not a billing or five-minute CLI
failure.

| Case | V4 Flash | V4 Pro |
| --- | --- | --- |
| Bayview, Aug 17 | investigate (11:44) | deadline (12:00) |
| Outside Lands trespasser, Aug 9 | investigate (10:07) | investigate (11:00) |
| Outside Lands traffic, Aug 7 | deadline (12:00) | investigate (11:42) |
| Presidio water rescue, July 25 | investigate (11:26) | investigate (10:45) |
| Haight control, Aug 6 | investigate (10:32) | investigate (8:56) |
| Already-covered Bayview, initial guidance | discard (10:35) | deadline (12:00) |
| Already-covered Bayview, earlier coverage gate | deadline (12:00) | discard (5:01) |

These are 14 valid model trials. An initial Flash Bayview attempt failed during
local image startup before model execution; it was preserved separately and
rerun. Nine core trials used `59c3b79`. The Flash Bayview rerun and initial
coverage pair used `95873bd`; the positive rerun adds an empty `priorCoverage`
field and updated coverage instructions. The revised coverage pair used
`7353223`. The mixed revision and single-trial design limit quality comparisons.
Input hashes, source revisions, raw scores, current scores, and archives are
retained; neither a wording correction nor a later successful run erases an
earlier result.

The initial Flash duplicate run drafted a later-date version, then discarded
it during final review. Pro's initial run timed out with three unfinished
artifacts, which the new archive code successfully preserved as **failed**, with
no completed result. The revised guidance says to check coverage before
expanding research and keep possible later developments as private revision
leads. Pro then discarded the case and named the existing article in 5:01;
Flash still drafted a standalone Aug. 8 article before timing out. Model
coverage guidance is therefore not a publication guarantee.

A truncation hypothesis was checked against the completed Flash read event:
it contained the full prior article body, including its Aug. 8 paragraph.
Missing delivery of that paragraph was not established as a cause.

The revised Pro discard initially failed a phrase check because it said
`covered pattern` across a line break. The checker now accepts that wording
and normalizes whitespace. The exact existing article slug is also required,
so generic discussion of duplicate CAD rows cannot satisfy the article-coverage
control. Raw scores remain unchanged in the evidence.

## Human review and evaluator limits

The automated checks are not publication approval. All completed article drafts
included a hero and self-review, yet review found errors those self-reviews missed:

- Pro's trespasser headline puts 24 incidents at the western edge while its own
  breakdown places only 23 there. Flash handles the row/incident distinction,
  but some baseline citations need upper date bounds.
- Both water-rescue drafts assert that a particular fire call was the response
  to the named recovery despite lacking a shared identifier or explicit source
  linkage. Pro also treats absence of a Medical Examiner disposition as proof
  that other calls were not fatal. These claims need correction or stronger evidence.
- Flash's Haight draft incorrectly makes overnight stops a subset of daytime
  stops in one sentence. Pro confuses police district with officers' station
  assignment and writes expected festival attendance as actual attendance.

The traffic-complaint Pro run's original phrase failure was a false positive:
`caller-originated` is a valid, more precise description than assuming the
callers were residents. The checker now accepts that wording; the original
score is retained in the evidence, with the corrected score reported separately.
Independent API checks confirmed all 145 weekend rows were on-view=N and the
reported disposition counts; three rows also carry duplicate links, which
requires care when distinguishing call-record volume from distinct incidents.

Both models advanced Haight beyond its watch-only expectation. Flash found a
broader recurring pattern: 823 stops through September 8 versus 376 in the same
2025 window; August totals were 203 versus 47. An independent live query also
confirmed the 23-stop daily high and 42-stop two-day high since January 2024.
Both drafts deny a demonstrated causal link to the nearby announcement.
The broader pattern makes this online control inconclusive as a measure of
publication eagerness. Its raw failure remains visible; the expected outcome
was not relaxed to make the comparison green. The new already-covered Bayview
case supplies an explicit, more stable reason to withhold a duplicate story.

## Production recovery after deployment

The production retries in this audit used the old deployed CLI and failed.
The code and migration in this PR must reach production before a new replay
can demonstrate that the production failure is repaired. Local model evals
cannot establish deployed ingestion freshness or Workflow recovery.

| Candidate | Next action |
| --- | --- |
| Mission Bay, August 26, `dispatch` / `TRAFFIC STOP` | Fresh replay; verify the 49-stop lead and its baseline. Earlier event connections remain unverified. |
| Chinatown, August 29, `311` / `Graffiti Private` | Fresh replay; distinguish recurring reporting/abatement routes from a new graffiti outbreak. |
| Haight Ashbury, August 6, `dispatch` / `TRAFFIC STOP` | Potential historical-pattern recovery after correcting and verifying the expanded finding above. |
| Bayview, August 8 | Keep the redundant standalone recovery discarded; the August 17 article already covers its main finding. |

Use new idempotency keys for deliberate post-deployment replays. Before each
publication, compare the full current feed and preserve an existing article's
slug when the work is a revision. The bounded prior-coverage context does not
replace that final manual backfill check. A generalized candidate backlog and
coverage-based readiness redesign are not part of this change.

## Validation and retained evidence

Local `pnpm check`, `pnpm test` (including Worker/D1 integration), `pnpm build`,
and `pnpm dev:smoke` passed. The citation migration was also tested against the
existing SQLite migrations, foreign keys, unique constraints, and four
production article documents; reapplying it added no extra revisions. All 13
published old-host source links returned HTTP 200 on the replacement host.
Paid evals are the mixed results above and are not run in CI.

The private R2 object
`public-patterns-archive/evals/2026-09-09/article-run-audit-01a08776.zip`
contains 76 result, archive, metadata, image, and independent-check files plus
a SHA-256 manifest. Raw Wrangler debug logs are excluded. The ZIP SHA-256 is
`087d811f9317f51b648b448fe81f319d0126a856da26c43f78dab28e4dc85887`.
No comparison article was published; the public feed remained at four articles
at the post-comparison check.
